name = "calroo"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".open-next/worker"

# Secrets go in Cloudflare Pages dashboard, not here.
# Required vars: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI,
#                SESSION_SECRET, OPENROUTER_API_KEY, NEXT_PUBLIC_APP_URL
